//! `gw-eval` — offline evaluation and dataset diagnostics.
//!
//! Read-only, GPU-free analyses over stored records: the grader separation diagnostic
//! (`decidable_fraction`, selector-vs-random on the reasoning axis) and the variance-aware
//! promotion gate. Depends on `gw-schema` and `gw-storage`.
